{
  "require": {
    "deployer/deployer": "^6.3.0",
    "php": ">=7.0"
  },
  "scripts": {
    "post-install-cmd": [
      "npm install; gulp build"
    ],
    "post-update-cmd": [
      "gulp build"
    ]
  }
}
